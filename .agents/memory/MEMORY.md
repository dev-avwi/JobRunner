- [LLM echoes prompt formatting](smart-import-ai-mapping.md) — gpt-5-mini returns enum values in the decorated format the prompt listed them (`name (Name)`); normalise when parsing + demand bare ids.
- [Security audit 2026-07](security-audit-2026-07.md) — SQLi sweep clean (one sql.raw hardened); HoundDog criticals false positives; audit severity is object `.level`; deps patched, transitive highs remain; SAST infra-cancels.
- [Offline cache logout wipe](offline-cache-logout-wipe.md) — every new mobile cache (SQLite table or subscription_cache key) must be added to clearCache() or it leaks across accounts; local_ jobs load cache-first then fall through to fetch.
- [Follow-up tasks (task-to-form linking)](job-tasks-followup.md) — task mutations strict ownerOnly (managers 403); gate task UI on strict owner; all 3 form-submission POSTs need taskRules hook + cross-tenant guards.
- [Subcontractor subscription gating (mobile)](subcontractor-subscription-gating.md) — subcontractor may buy Pro ONLY (Team/Business hidden, no team to manage); host-workspace context hides upgrade entirely; gate on isSubcontractor + isStandaloneSubcontractor; UI-only.
- [Invite-code subcontractor role fallback](invite-code-subcontractor-role-fallback.md) — sub invite codes set roleType not roleId; redeem fell back to Worker + missing accountType; create role + set accountType on redeem.
- [Mobile role label falls back to Worker](mobile-role-config-missing-subcontractor.md) — team-management.tsx ROLE_CONFIG is the only mobile role-label map; a missing key (e.g. subcontractor) badges Worker even when DB role is correct. Add a key per assignable role.
- [Mobile Android Google Maps setup](mobile-google-maps-android.md) — "API key not found" = key/project binding + prebuild-rebuild required; also user's local expo run:android workflow + emulator-only IAP/SQLite errors.
- [Mobile dev sync model](mobile-dev-sync-model.md) — user runs Metro on own Mac clone; agent edits reach device only after git pull + reload; "still broken" screenshots usually = stale local code.
- [Guarding optional Expo native modules](mobile-optional-native-module-probe.md) — top-level requireNativeModule throws past try/catch; probe with requireOptionalNativeModule first; feature still needs native rebuild.
- [Mobile TextInput placeholder spread](mobile-textinput-letter-spacing.md) — RN inputs render placeholder with big letter-spacing on device; fix = explicit letterSpacing:0 + textAlign:'left' (typography.body now carries it; literal-style inputs need it per call-site).
- [Mobile logout redirect race](mobile-logout-redirect-race.md) — un-awaited logout()+navigate to '/' races index.tsx (sees stale isAuthenticated=true) → stuck on dashboard; await logout() then replace('/(auth)/login'); wrap store cleanup in try/finally.
- [Mobile feature broken = stale prod deploy](mobile-prod-stale-deploy.md) — app reports "not joined"/feature broken but data is correct → live backend older than app (PROD_FALLBACK). Probe prod for 404s; fix is publish, not code.
- [Mobile location foreground fallback](mobile-location-foreground-fallback.md) — worker never on team map: startTracking catch must fall back to a foreground send for ANY bg-start failure (iOS "While Using" throws a non-matching error string), not a specific message.
- [Delayed modal handoff stale closure](delayed-modal-handoff-stale-closure.md) — 400ms modal-collision setTimeout runs the pre-update closure (stale amount); snapshot values or use latest-ref (fnRef.current) for the delayed call.
- [Bottom sheet drag steals taps](sheet-pan-steals-taps.md) — AppBottomSheet pan threshold too low cancels row taps mid-press (blind first tap); keep dy>24 + 2x vertical dominance, NO velocity gate.
- [Mobile confirm dialogs](mobile-confirm-dialogs.md) — use existing useConfirmDialog() hook (iOS native Alert, Android/web branded centered modal); don't write new Alert.alert or dialog components.
- [iOS 26 empty headerRight circle](ios26-empty-headerright-circle.md) — empty (non-null) native-stack headerRight renders a hollow glass circle on iOS 26; return null when a role has no header actions, never an empty View.
- [Force light theme on one route](theme-force-light-route.md) — ThemeProvider writes inline CSS vars on documentElement + re-runs; lock a route light via root var override + MutationObserver, not a one-shot class flip.
- [Onboarding business-settings + seeding](onboarding-business-settings.md) — trade templates seed ONLY on POST /api/business-settings carrying tradeType; create is non-idempotent (PATCH-then-POST-on-404); profile = /api/profile/me, non-blocking.
- [Onboarding seeding web vs mobile](onboarding-seeding-web-vs-mobile.md) — web wizard uses seed-sample-data (ownerOnly, isSample); mobile uses legacy seed-demo-data (requireAuth). Don't delete seed-demo-data as "dead" — mobile still calls it.
- [Deploy build fails at puppeteer Chrome download](deploy-puppeteer-chrome-download.md) — local build clean but autoscale publish dies "while bundling"; puppeteer's install-time Chrome download is the culprit. Fix: .puppeteerrc.cjs skipDownload + system chromium.
- [GuidedTour advance mechanism](guided-tour-advance.md) — interactive steps advance via wouter route change ONLY (no click listener) or double-advance skips steps; form steps use allowInteraction + advanceOnRoute gate.
- [SendGrid click-tracking cert warnings](sendgrid-click-tracking-cert.md) — "not secure"/CERT_COMMON_NAME_INVALID on email links = SendGrid click-tracking rewriting through broken-cert subdomain; keep clickTracking disabled, openTracking on.
- [Deploy publish fails — image over 8 GiB](deploy-image-size-8gib.md) — "image size is over the limit of 8 GiB" shows in the UI build log, NOT fetchDeploymentLogs. Slim via scoped build-cmd rm -rf of non-runtime dirs; NEVER .cache/.local.
- [Email case-normalization / dup accounts](email-case-normalization.md) — users_email_unique is CASE-SENSITIVE; two accounts can share one email. Normalize email lower+trim on every write; match lower(email) on every read.
- [Invite accept vs cross-business email conflict](invite-accept-email-conflict.md) — trusted-invite token must bypass cross-business email conflicts (client/other-invite rows); keep the existing-user block.
- [Settings tab role scoping](settings-tab-scoping.md) — workers only get Account/Appearance/Support; Notifications tab saves to owner-only /api/integrations/settings so don't expose it; gate via 3 places (savedTab allowlist, isAlwaysAvailable, TabsTrigger).
- [business-settings worker sanitization](business-settings-worker-sanitization.md) — GET /api/business-settings for non-owners MUST allowlist display fields, never spread the raw owner row (it holds tokens/bank/stripe PII).
- [No storage.db property](storage-db-undefined-footgun.md) — `db` is module-level in storage.ts; `storage.db.*` is undefined at runtime and slips past the non-typechecking esbuild build. Use storage methods.
- [PressableRow percentage collapse (iOS)](pressablerow-percentage-collapse.md) — % width/maxWidth on PressableRow double-applies and collapses; fix per call-site, don't touch the shared component.
- [Mobile live-location first ping](mobile-live-location-first-ping.md) — enabling team location must force one immediate send; OS background task only fires after ~50m, so stationary workers never show on owner map.
- [Client portal live-location sources](portal-live-location-sources.md) — /p/:token dot reads workerTravelLocations + location_pings, NOT team-locations; bridge en_route team-location POSTs into both. location_pings.userId NOT NULL.
- [SDS Manager integration](sds-manager-integration.md) — integrate, don't rebuild; they have AU-compliant SDS DB + Parser API + QR access; copy their SafetyCulture model; needs partner API key first.
- [Job start gate paths](job-start-gate-paths.md) — a gate on starting a job must run on ALL status writers (/status, full update, bulk-status), not just /status; resolve assignedTo to user id first.
- [id-only storage IDOR](idor-id-only-storage-writes.md) — id-only writes: confirm pre-check/route ownership/token gate per call site + strip raw-body mass-assignment; reads: see [read-IDOR getters](idor-read-id-only-getters.md).
- [Paid-tier gating siblings](paid-tier-gating-siblings.md) — locking a feature to paid = gate EVERY write/action route in the group (accounting has ~35), not just connect/sync; leave callbacks/webhooks/status/disconnect/test open.
- [403 must not clear session](auth-403-clears-session.md) — Bearer-token-only auth; non-owners 403 on owner-only endpoints; clearing token on 403 silently logs them out on reload. Only 401 clears.
- [Onboarding guard](onboarding-guard-blocks-jobs.md) — onboardingCompleted=false 403s /api/jobs (cached; restart after DB patch); exempt list + welcome email details in [exempt-and-welcome](onboarding-guard-exempt-and-welcome.md).
- [Mobile offline SQLite init NPE](mobile-sqlite-init-race.md) — prepareAsync/execAsync NullPointerException = concurrent double-init race; fix is an initPromise singleton guard; graceful fallbacks must warn not error.
- [Adding a DB table: don't db:push](db-add-table-no-push.md) — drizzle-kit push wants destructive drops (users.role etc) on this DB; add new tables via raw CREATE TABLE IF NOT EXISTS, not npm run db:push.
- [Mobile auth-error strings](mobile-auth-error-strings.md) — /api/auth/me returns "Not authenticated" for dead token; auth detection must match all 401 wordings case-insensitively (isAuthErrorMessage in api.ts) or stale cached session locks the app.
- [Mobile typecheck deps drift](mobile-typecheck-deps-drift.md) — local typecheck.sh false-cleans if node_modules drifts from lockfile; run `npm --prefix mobile ci` first (CI installs locked @types/react 19 + TS 5.9).
- [Mobile node_modules reinstall in Replit](mobile-shell-quote-firewall-block.md) — firewall blocks shell-quote; restore via curl tarball + install as a workflow + drop it in by hand. Never npm ci mobile here.
- [Routes missing requireAuth 401 on mobile](auth-routes-missing-requireauth.md) — route reading req.userId||req.session?.userId with NO requireAuth works on web (cookie) but always 401s mobile (Bearer); silent empty state. /api/auth/* family was the offender.
- [Mobile api.get returns error body as res.data](mobile-api-error-body-stored-as-data.md) — non-2xx returns {error, data:errorBody}; `if(res.data) setX(res.data)` stores the error obj → undefined-leaf render crash. Gate on !res.error + validate/normalize shape.
- [Custom queryFn bypasses Bearer token](custom-queryfn-bypasses-bearer.md) — web app is Bearer-only; a hook's own raw fetch(credentials:'include') 401s silently → wrong free-tier/empty fallback (locked Map for team worker).
- [Standalone subcontractor labelling](standalone-subcontractor-account-type.md) — business_settings.accountType='subcontractor' makes my-role owner-fallback return roleName 'Subcontractor' (isOwner stays true); membership always overrides; prod needs the column.
- [Team-capable tiers + standalone-subbie block](team-capable-tiers.md) — only Team/Business(+beta) can have members (Pro is single-user despite TIER_LIMITS Pro=1; ownerHasTeamCapability is authoritative); subbie onboarding now invite-mandatory on web AND mobile.
- [users.role is vestigial](users-role-vestigial.md) — DB col defaults to 'patient' (medical-template leftover), NOT in Drizzle schema, never read by access control; owners can have multiple business_settings rows (handled).
- [Paid gate owner-context](paid-gate-owner-context.md) — requireProSubscription reads the logged-in member's own tier (blocks paid-business workers); use requirePaidTier() which resolves to the owner.
- [RBAC gating audit conventions](rbac-gating-audit.md) — owner/demo always pass gates; template creation needs MANAGE_TEMPLATES not ownerOrManagerOnly (managers have MANAGE_TEAM); fix IDOR-by-id at route layer with userId 404 check.
- [Payment-confirm trust](payment-confirm-trust.md) — any "mark paid" route must verify the Stripe PI succeeded + amount/currency + ownership; demo bypass gated on receiving business being demo, never a client flag.
- [users table has no role/businessOwnerId columns](users-table-no-role-owner-columns.md) — gating on user.role or user.businessOwnerId fails open (always undefined); use getUserContext (effectiveUserId / MANAGE_TEAM / VIEW_ALL).
- [Subbie billing rules](subbie-billing.md) — price labour off each time_entry hourlyRate (not membership rate; null rate = $0 invoice); exclude isBreak entries in every billing path; dashboard earnings (live, membership rate) can differ from frozen invoices.
- [Scheduler N+1 starves Neon pool](scheduler-n1-pool-starvation.md) — "Connection terminated due to connection timeout" from a background job = per-row N+1 loop saturating max-15 pool; fix with grouped aggregates, not DB tuning.
- [Mass-assignment on scoped updates](mass-assignment-scoped-updates.md) — (id,userId)-scoped WHERE still needs req.body stripping (id/userId/businessOwnerId/timestamps); strip pattern + audit false-positives.
- [Mobile Android native-component gotchas](mobile-android-native-component-gotchas.md) — DateTimePicker is a native dialog (clear show flag in onChange or it loops); nested native header needs headerStatusBarHeight:0 on Android (global Header already ate the inset).
- [Subscription & billing gating](subscription-billing-gating.md) — all /api/subscription/* and /api/billing/* lifecycle mutations must be ownerOnly() not bare requireAuth; whitelist client-supplied trial tier ('pro'|'team' only) or 'business' grants free access.
- [Object storage serving model](object-storage-serving-model.md) — open /objects/ route only authorizes 3 chat prefixes + public uploads/<uuid>; photos/voice notes use signed URLs; default-deny other .private/. Don't switch to canAccessObject (no ACL metadata).
- [Object storage read-URL signing](object-storage-signing-sidecar.md) — SDK getSignedUrl throws "Cannot sign data without client_email" on Replit; sign via sidecar (getSignedReadURLFromKey); SDK read/write fine.
- [business_settings write schema](business-settings-mass-assignment.md) — POST/PATCH /api/business-settings must omit server-controlled billing/stripe/seat/trial/onboarding cols; raw insert schema only omits id/userId/timestamps → self-upgrade hole.
- [Session revocation currentSid](session-revocation-currentsid.md) — "log out other devices" must derive currentSid like requireAuth (cookie session first, Bearer fallback) or you wipe the user's own current session.
- [Mobile Live Activity lifecycle](mobile-live-activity-lifecycle.md) — lock-screen timer activity is anchored in store.stopTimer; real stops must end() it, pause/resume pass keepLiveActivity to flip not end. New stop paths must go through stopTimer.
- [Owner self-membership misclassification](owner-self-membership-misclassification.md) — owner 403s on owner-only routes when self team_members row + empty/dup business_settings + null active_business_id; keep getBusinessSettings ordered + guard self-memberships in getUserContext.
- [Permission gating middleware](permission-gating-middleware.md) — use createPermissionMiddleware(PERMISSIONS.X) over ownerOrManagerOnly (=MANAGE_TEAM, not a generic manager gate); coarse gate only when no granular perm exists.
- [Subscription/trial status contract](billing-trial-status.md) — trial = status:'trialing'+isTrial, NOT tier:'trial'; getSubscriptionStatus returns real tier; mobile subscription.tsx breaks if you change the contract.
- [Standalone full-screen routes](standalone-route-rendering.md) — full-screen pages (invite/assignment accept) need an own short-circuit BEFORE the app shell or they render embedded in sidebar/header when logged in; accept uses hard reload.
- [Team invite accept broadcast](team-invite-accept-broadcast.md) — owner Team page stuck on "Pending" after accept unless the accept path broadcasts team_member_changed; 4 accept paths; same-email/2-business pending is correct, not a bug.
- [Android card halo + job double header](mobile-android-card-halo-and-double-header.md) — Android-only: elevation on tinted cards = gray halo (zero xs/sm/md); job screen stacks global+native header (hide native on Android, in-content Back row); grid 1-col → width '48%'.
- [Portal ETA staleness + GPS flow](portal-eta-staleness.md) — recompute ETA from live location each poll (cache OSRM 30s); static "20 min" = worker GPS never reached server, see [onmyway-eta-gps-flow](onmyway-eta-gps-flow.md).
- [Mobile owner tracking-window gating](mobile-location-window-gating.md) — boot must call store.initializeTracking (not hook) or scheduler/permissionGranted never set; override on all timer paths + hydrate; overnight days use prevDay for the morning portion.
- [OAuth onboarding routing via isNewUser](oauth-onboarding-isnewuser.md) — route brand-new mobile OAuth users to onboarding by server isNewUser flag, NOT onboardingCompleted; dashboard spinner cards self-recover.
- [Permission vocabulary mismatch](permission-vocabulary-mismatch.md) — roles store granular verbs (create_clients/view_jobs) but routes check coarse (write_clients/read_jobs) → correct-looking 403; bridge via additive expandPermissions in getUserContext.
- [Frontend route + nav gating is role-only](frontend-route-nav-gating.md) — worker custom permissions unlock a page ONLY if you additively patch BOTH canAccessRoute AND filterNavItems (+3 nav consumers); /team≠/team-operations (real editor).
- [RouteGuard deny-by-omission](routeguard-deny-by-omission.md) — a client route with no PAGE_PERMISSIONS entry (permissions.ts) silently redirects to dashboard on mount; page's own query never fires. Register every new App.tsx route.
- [fetchTeamRole Bearer token missing](fetch-team-role-bearer.md) — use-user-role.ts custom queryFn used credentials:include (no Bearer token); permissions silently empty in browser. Pattern repeats in ~15 other hooks.
- [Logger wrong-signature alert flood](logger-signature-alert-flood.md) — admin gets spammed transient DB-timeout alerts when a caller does logger.error(message, error) instead of (category, message, {error}); bypasses isTransientInfraError suppression.
- [Feature roadmap — Jobber gaps](feature-roadmap-jobber-gaps.md) — buildable candidates from a Jobber comparison (website builder, marketing/competitor analysis, AI advisor, job costing, geofenced timers); priority = website builder + job costing.
- [Pricing — live tiers + how to change a price](pricing-plan.md) — live AUD Pro 49.99/Team 99.99/Business 199.99 (set 2026-06-27); changing a price MUST bump the Stripe lookup_key (v2→v3) or it charges the old amount; many display literals are hardcoded.
- [SMS conversation access sweep](sms-conversation-access-sweep.md) — gating a conversation means ALL /:id sub-routes (incl. /client-insights, /messages, and DELETE which had NO check = IDOR), not just the main GET; use workerCanAccessConversation.
- [Worker multi-worker job visibility](worker-multiworker-assignment-visibility.md) — worker assigned to a 2+ worker job sees 0 assigned; filters checked only jobs.assignedTo (1 lead) not job_assignments. OR in getAssignedJobIdsForUser across all job-list endpoints.
- [Job-list endpoints VIEW_ALL gate](job-list-endpoints-viewall-gate.md) — map/dispatch/any job-returning endpoint must reuse the /api/jobs `hasViewAll` filter, NOT an isSubcontractor check; staff tradies leaked all jobs on the map otherwise.
- [Clients list VIEW_ALL gate](clients-list-viewall-gate.md) — /api/clients returned empty for office roles (Office Manager) gated only on view_all; full book must also pass read_clients_sensitive (the view_clients grant) + `*`; field tradies (bare read_clients) stay narrowed.
- [Reports summary partial payload](reports-summary-partial-payload.md) — /api/reports/summary can return partial nested data despite non-optional type; normalise the whole summary once with ?? defaults, not a single optional-chain.
- [Android foldable layout](mobile-foldable.md) — isTablet() must use window dims (Android) not screen; responsive nav from useWindowDimensions(); bottom-sheet needs Modal key on dims + flexShrink ScrollView.
- [Android map markers never paint](mobile-android-map-markers-tracksviewchanges.md) — custom-view <Marker> with tracksViewChanges=false from first mount stays invisible on Android; gate per-platform + brief true-then-false state. iOS keeps false (anti-flash).
- [Trials: once-per-user + tier sync](trial-abuse-prevention.md) — gate every trial-granting path with hasUsedTrial; trial tier syncs via reconcile-from-Stripe on success redirect, see [stripe-trial-tier-not-syncing](stripe-trial-tier-not-syncing.md).
- [Mobile typecheck vs stale router types](mobile-typecheck-stale-router-types.md) — new mobile/app route fails tsc as TS2345 until expo dev server regenerates .expo/types/router.d.ts; boot expo briefly, dont cast to any.
- [Mobile role label & nav gating](mobile-role-nav-gating.md) — dashboard "Team member" vs profile "Owner" = store.fetchRoleInfo owner-detect must match useUserRole (roleName.includes('owner')); simple_mode defaults TRUE so hideInSimpleMode+paid-gate = wrong "Team plan" upsell.
- [Push + in-app notification double-post](push-inapp-double-post.md) — sendPushNotification ALSO creates an in-app notification unless skipInAppNotification:true; pairing it with a manual createNotification double-posts to the owner's bell.
- [tsc excess-prop masking](tsc-excess-prop-masking.md) — TS2353 reports only the FIRST excess prop per literal; removing it surfaces the next, so re-run check iteratively. esbuild prod build skips typecheck so server type errors are real bugs.
- [Vapi rollout + config write paths](vapi-webhook-secret-rollout.md) — webhook secret is verbatim (prod-first rollout order); new assistant fields must thread through ALL write paths, see [vapi-assistant-write-paths](vapi-assistant-write-paths.md).
- [runTest canonical URL dead port](runtest-canonical-url-dead-port.md) — runTest 502s: canonical URL maps to stale port 23636; forwarder workaround. Also full-reload-per-route trips 429 → marketing page (env artifact, not a bug).
- [gpt-5-mini param constraints](gpt5-mini-param-constraints.md) — migrating OpenAI calls to gpt-5 family: rename max_tokens→max_completion_tokens, drop custom temperature, bump small budgets (reasoning eats them); Vapi aiModel is separate.
- [Readiness-gate deadlock](readiness-gate-deadlock.md) — a "ready" flag that gates deferred UI must flip in finally (even on fetch reject), else one failed load suppresses the gated UI for the whole session.
- [Role-gated UI flicker](role-ui-flicker.md) — sign-out/sign-up/foreground all briefly resolve wrong role (cache cleared or 404 placeholder); gate on signingOut flag / authoritative user.isOwner / latch visibility, never transient role fallback.
- [Onboarding invite-code length](onboarding-invite-code-length.md) — codes are 6 chars (server INVITE_CODE_LENGTH=6); validateInviteCode guard must be !==6 not 8 or Connect&Continue stays disabled (worker+sub). DB column comment says 8, stale. Also: dedupe skip buttons.
- [Personal profile workspace gating](workspace-personal-profile-gating.md) — my-businesses hides Personal profile for pure workers (show only owners/subcontractors); don't fake activeBusinessId, switcher disables the active card.
- [Subcontractor dashboard](subcontractor-dashboard.md) — activeJob from the running time entry (not assigned in_progress filter); screen must call fetchActiveTimer itself or break state is stale.
- [time_entries has no tenant column](time-entries-no-tenant-column.md) — business-wide active-timer/time-entry queries by userId leak across businesses for multi-membership workers; verify job ownership per non-self entry.
- [Brand button contrast](mobile-brand-foreground-contrast.md) — prefer white text at >=3.0 contrast on brand colors; also [primaryForeground rule](mobile-primary-button-white-text.md) and [SheetButton for iOS modal footers](mobile-brand-button-white-on-white.md).
- [Worker presence state blast radius](worker-state-blast-radius.md) — adding/removing a worker state must touch server validStates, shared WORKER_STATES/_CONFIG, mobile MY STATUS pills+display colors, AND team-operations pill/subtitle chain (defaults to "Available" otherwise).
- [Job completed status vocab](job-completed-status-vocab.md) — finished job is status 'done' (+ 'invoiced'), NEVER 'completed'; filtering on 'completed' silently returns 0/empty (bit the subbie dashboard jobs-completed stat).
- [Worker status: three stores](worker-status-three-stores.md) — availabilityStatus (subbie pill) vs worker_states (owner board reads THIS) vs team_presence don't auto-sync; mirror availability into worker_states + broadcast.
- [Mobile has no general WS event bus](mobile-no-ws-event-bus.md) — only map.tsx has a (location) socket; server broadcasts reach no listener. Live-updating screens must useFocusEffect refetch + poll, not rely on WS.
- [Booking page funnels into Leads](booking-page-funnels-into-leads.md) — public /book/:slug creates a Lead (source booking_page), no separate booking inbox; all intake (booking page, website form, AI receptionist) converges on Leads.
- [Autopilot/automations engine is real](automations-engine-real.md) — not a stub; event triggers fire inline + 30min time-based poller (reminderScheduler), executeAutomationActions runs real sends; custom builds execute same as templates.
- [Image picker base64:true crashes Android](mobile-imagepicker-base64-android-crash.md) — expo-image-picker base64:true → ANR/BadParcelableException; pick to URI then FileSystem.readAsStringAsync Base64 instead.
- [expo-av single-recording orphan](expo-av-single-recording-orphan.md) — one prepared Recording per process; orphan across remount blocks next record. Module-level globalRecording, clear only after successful unload.
- [Lead-worker job completion gate](lead-worker-completion-gate.md) — only lead/owner may mark job done (server NOT_LEAD_WORKER); mobile lead test must wait for assignmentsLoaded; sync drops NOT_LEAD_WORKER job updates.
- [Bill-for-breaks setting](bill-breaks-setting.md) — owner billBreaks toggle bills break time on invoices; any worker-read setting must ALSO be added to WORKER_VISIBLE_SETTINGS_KEYS, not just the write schema, or worker sessions resolve it to default.
- [Feature-permission backfill](feature-permission-backfill.md) — server-gating a menu-visible feature needs an additive+idempotent DB backfill of manager/office-admin role rows (granular view_* keys) or they 403 despite seeing the menu.
- [CodeQL alert remediation patterns](codeql-remediation-patterns.md) — exact fix shapes for biased-random/format-string/SSRF/type-confusion/host-header; host-header must allowlist req host vs REPLIT_DOMAINS not delete fallback; audio-fetch SSRF is a false positive to dismiss.
- [Mobile active-workspace-owner signal](mobile-active-workspace-owner-signal.md) — active business owner id is on user.businessOwnerId (from /api/auth/me), NOT roleInfo; use for cross-workspace gates. stopTimer returns false on fail (no throw).
- [Push-token device exclusivity](push-token-device-exclusivity.md) — device kept getting prev account's pushes (overtime nudge) after account switch; logout must DELETE /api/push-tokens (unbind) BEFORE api.logout, run first in own try/catch.
- [Voice note recording cap](voice-note-recording-cap.md) — both recorders auto-stop at 5min; cap is coupled to the 10MB express.json body limit (voice notes upload as base64 JSON, not multer). Raise bitrate-or-limit together, not just cap.
- [jobs.notes PATCH writes are dead](jobs-notes-write-path-dead.md) — PATCH /api/jobs/:id strips notes (moved to job_notes table); write via POST /:jobId/notes, read BOTH sources; mobile editor still broken.
- [getX includeArchived is archived-only](getx-includearchived-archived-only.md) — storage.getQuotes/getInvoices/getJobs(id,true) returns ONLY archived, (id,false) ONLY active; merge both for a full set. Caused a false demo-corruption diagnosis.
- [ActionSheet grid even-width in bottom sheet](actionsheet-grid-scrollview-width.md) — grid renders inside AppBottomSheet's ScrollView (useAutoHeight) so % widths bunch/collapse; measure via onLayout + pixel width = floor(gridWidth/perRow).
- [Custom-form submit has two routes](custom-form-submit-two-routes.md) — /api/form-submissions (web, routes.ts) vs /api/custom-forms/:id/submit (mobile, routes/custom-forms.ts); change form access + submittedBy in BOTH; mobile one wrote owner id.
- [Upload paths web vs mobile](upload-paths-web-vs-mobile.md) — use POST /api/upload (returns servable /objects/ {url}, accepts PDF); presigned pathname stored raw is NOT viewable; multipart needs raw fetch+Bearer. Also: max upload size (now 100MB) is a COUPLED ceiling (multer×2 + client checks + /api timeout + memoryStorage RAM bounded by uploadQueue).
- [Completion modal mode must be set on every open path](completion-modal-mode-paths.md) — any path that opens the job completion modal (geofence ?action=complete, CTA, action bar) must set completionMode worker/owner first, and wait for assignmentsLoaded; store updateJobStatus fails silently (returns false) so callers must toast store.error.
- [Offline cache fallback gating](offline-cache-fallback-gating.md) — detail screens falling back to SQLite cache must gate on response.isOffline ONLY (api.ts sets it), never on any error, or cached data masks online 401/403/404 denials.
- [Play foreground-service flags](play-foreground-service-flags.md) — flags come from unused deps' merged manifests (expo-audio); fix = remove dep via lockfile surgery, never npm install in mobile.
- [Offline cold-start role seeding](offline-role-cache-seeding.md) — roleCache is in-memory only; offline relaunch left role 'loading' forever → More menu permanent skeleton. Seed from persisted cached_auth roleInfo with timestamp 0 (stale-while-revalidate).
- [Stripe Terminal Tap to Pay aborts](stripe-terminal-simulator-abort.md) — discoverReaders abort()s on simulated:false in simulator AND on stale 'localMobile' method (renamed 'tapToPay' in beta.29; unknown method silently falls back to Bluetooth → crash on real devices).
- [Quick Collect job linking](quick-collect-job-linking.md) — no-invoice tap must thread quickCollectJobId into collectPayment + clear it on EVERY exit path; "Paid" badge/confirm is screen-only (never mutate job status); history enriches clientName/invoiceNumber/jobTitle.
- [Terminal payment-success ordering](terminal-payment-success-ordering.md) — side effects BEFORE marking succeeded; PI id = idempotency key; terminal PIs are DIRECT charges on the connected account — create/retrieve/cancel all need stripeAccount or "No such payment_intent".
- [Receipt PDF template passthrough](receipt-pdf-template-passthrough.md) — receipts have no template column; style comes only from the business blob's documentTemplate/documentTemplateSettings, so narrowed literals silently fall back to the DEFAULT template.
- [Catalog item CRUD routes](catalog-crud-routes.md) — /api/catalog was create-only (edit/delete 404d for everyone though storage methods existed); feature is per-user (req.userId) not per-business; id-only getter needs fetch-then-owner-check on writes.
- [Sentry noise triage](sentry-noise-triage.md) — weekly-report errors are mostly noise (Playwright bot frames, dev EADDRINUSE, mobile offline events landing in node-express project); pull event via API + check tags before fixing.
- [Unified payment history](payment-history-unification.md) — buildPaymentHistory merges terminal_payments (Tap to Pay) + the RECEIPTS ledger (links/QR/card/cash/etc), NOT payment_requests (paid links live as receipts; the request row rarely flips to 'paid'); dedupe by PI prefers succeeded; owner-scoped (no businessOwnerId col).
- [Mobile bottom-nav padding](mobile-bottomnav-padding.md) — root layout already pads content by bottomNavHeight; adding it per-screen double-pads (big empty strip). Use plain spacing only.
- [Offline form-submission sync](offline-form-submission-sync.md) — offline delete of a cached server row needs a tombstone (pending_sync=1/sync_action='delete') or write-through cache resurrects it; clear tombstone only after server confirms.
- [Manager near-owner access](manager-near-owner-access.md) — manage_team aliases to view_all (NOT assign_jobs); my-role returns roleName not role for members; gate on permissions, never role-name regex server-side.
- [TTP Apple system terms sheet](ttp-apple-system-terms-sheet.md) — Apple requires their own iOS T&C sheet (shown on first tapToPay reader connect); setup now connects for real; custom terms reworded as JobRunner terms.
- [Form submission edit/versioning](form-submission-versioning.md) — job-card edits snapshot prior data (fail-loud), atomic version numbering; reused $1 param needs ::varchar cast (42P08).
- [Android billing library strip](android-billing-library-strip.md) — Android is Stripe-links only; react-native-iap excluded from Android build (react-native.config.js) to satisfy Play Billing 8 rule; never re-add store flavor without migrating to expo-iap.
- [Subbie invoice card payment links](subbie-invoice-payment-links.md) — reuse payment_requests; refuse platform-charge fallback, atomic pending→paid flip, cancel on all terminal states; prod needs subcontractor_invoice_id ALTER.
- [Subcontractor billing role gate](subbie-billing-role-gate.md) — /api/subcontractor billing writes must check role name includes 'subcontractor', not just active membership; custom items = arbitrary amounts.
- [queryClient offline fallback vs sub-resource keys](queryclient-offline-detail-fallback.md) — web default queryFn offline fallback returned cached parent OBJECT for ['/api/jobs', id, 'sub'] list keys → ".filter is not a function"; detail = exactly 2 segments.
- [NDIS item codes + numbering](ndis-item-codes-numbering.md) — 5 prod ALTERs pending at publish; sequential counters must reserve via atomic UPDATE...RETURNING, never select-then-update; new quote→invoice paths must thread itemCode.
- [TTP App Review Stripe swap](ttp-review-stripe-swap.md) — prod demo account holds Ayden's live Stripe Connect account (done for Tap to Pay review); user chose to KEEP it after 1.1.5 approval — do not reverse unless asked.
- [Deploy blocked by dependency vulns](deploy-blocked-dependency-vulns.md) — publish scan reads ALL lockfiles incl mobile/; fix transitive CVEs via lockfile surgery + tarball drop-in, not installs.
- [WebSocket auth tickets](ws-auth-ticket.md) — web is Bearer-only and WS upgrades cannot carry headers; use the single-use /api/ws-ticket nonce flow, never cookies or raw tokens in the URL.
- [Import traceability (import_runs)](import-traceability.md) — every import path must open+finalize an import_runs row and tag created rows (incl. side-created clients) or history/undo misses them.
- [Private compliance attachment links](compliance-attachment-links.md) — /objects/.private/compliance links 401 from plain <a href> (web is Bearer-only); open via POST /api/objects/sign-download then window.open. Prod needs attachment_url/attachment_type ALTERs on swms_documents + training_records at publish.
- [iOS IAP gotchas](iap-product-must-be-fetched.md) — fetch products before buy; outcome is async via listeners (swallow E_USER_CANCELLED, see iap-cancel-async-outcome.md); paid add-ons rules in apple-iap-addons.md.
- [Team map gotchas](map-team-locations-endpoint.md) — mobile map status source + decoy endpoint; also stale current_job_id hides subbies (map-subcontractor-stale-current-job.md), geofence events need coords (geofence-event-coords-required.md), simulator GPS defaults to California (simulator-default-gps.md).
- [Mobile sidebar gotchas](mobile-sidebar-unknown-role-empty.md) — unknown roles must fall back to staff or menu empties; icon alignment math (sidebar-icon-alignment.md); team gate must derive from teamSize not nonexistent hasTeam (sidebar-team-gate-hasteam.md).
- [Sheet sync one-way export](sheet-sync-one-way.md) — settings mutate only via /api/sheet-sync/*; any state that makes google_sheets target unusable must also force-disable the sync server-side.
- [drizzle-zod coercion traps](drizzle-zod-timestamp-string-dates.md) — timestamp cols reject ISO strings (z.coerce.date) and decimal cols reject numbers (coerce to string server-side, see drizzle-zod-decimal-number-rejected.md); both = silent 400s.
