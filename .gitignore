node_modules
.env
.env.*
.expo
dist
build

# Exclude from deployment bundle (large directories not needed in production)
attached_assets/
mobile/ios/
mobile/android/

# Signing secrets — NEVER commit (certificates, keystores, provisioning, passwords)
mobile/credentials.json
mobile/credentials/
**/*.p12
**/*.p8
**/*.jks
**/*.keystore
**/*.mobileprovision
mobile/GoogleService-Info.plist
mobile/google-service-account.json

# macOS Finder metadata — never commit these
.DS_Store
**/.DS_Store
