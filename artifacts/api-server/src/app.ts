// This file is kept for backwards compatibility but the main server setup
// is done in index.ts which integrates the full legacy server setup.
// The Express app is created inside index.ts directly.
