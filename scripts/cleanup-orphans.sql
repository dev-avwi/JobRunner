-- Auto-generated by scripts/gen-cleanup.py. DO NOT EDIT BY HAND.
-- Resolves orphaned rows (children referencing deleted parents) so the foreign
-- keys declared in shared/schema.ts can be created by `drizzle-kit push`.
--
-- Remediation matches the declared FK behavior:
--   * onDelete 'set null'  -> set the column to NULL (preserve the record)
--   * onDelete 'cascade'   -> delete the orphan row (the parent is gone)
--   * otherwise            -> NOT NULL column: delete; nullable column: set NULL
--
-- Safe + idempotent: a no-op once the cascade FKs exist. It loops to a fixpoint
-- because resolving one orphan can expose orphans in tables that referenced it,
-- and aborts if a single pass would touch an implausibly large number of rows
-- (guards against an accidental mass-mutation from a future schema bug).
DO $$
DECLARE rc int; total int; passes int := 0; MAX_PER_PASS constant int := 100000;
BEGIN
  LOOP
    total := 0; passes := passes + 1;
    DELETE FROM "business_settings" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "business_settings" rows (missing "users")', rc; END IF;
    DELETE FROM "integration_settings" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "integration_settings" rows (missing "users")', rc; END IF;
    DELETE FROM "notifications" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "notifications" rows (missing "users")', rc; END IF;
    DELETE FROM "push_tokens" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "push_tokens" rows (missing "users")', rc; END IF;
    DELETE FROM "activity_logs" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "activity_logs" rows (missing "users")', rc; END IF;
    DELETE FROM "team_presence" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_presence" rows (missing "users")', rc; END IF;
    DELETE FROM "team_presence" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_presence" rows (missing "users")', rc; END IF;
    UPDATE "team_presence" c SET "current_job_id" = NULL WHERE c."current_job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."current_job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "team_presence"."current_job_id"', rc; END IF;
    DELETE FROM "activity_feed" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "activity_feed" rows (missing "users")', rc; END IF;
    UPDATE "activity_feed" c SET "actor_user_id" = NULL WHERE c."actor_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."actor_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "activity_feed"."actor_user_id"', rc; END IF;
    UPDATE "activity_feed" c SET "team_member_id" = NULL WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "activity_feed"."team_member_id"', rc; END IF;
    DELETE FROM "clients" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "clients" rows (missing "users")', rc; END IF;
    DELETE FROM "jobs" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "jobs" rows (missing "users")', rc; END IF;
    DELETE FROM "jobs" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "jobs" rows (missing "clients")', rc; END IF;
    UPDATE "service_reminders" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "service_reminders"."job_id"', rc; END IF;
    DELETE FROM "service_reminders" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "service_reminders" rows (missing "clients")', rc; END IF;
    DELETE FROM "service_reminders" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "service_reminders" rows (missing "users")', rc; END IF;
    DELETE FROM "checklist_items" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "checklist_items" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_checkins" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_checkins" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_checkins" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_checkins" rows (missing "users")', rc; END IF;
    DELETE FROM "quotes" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quotes" rows (missing "users")', rc; END IF;
    DELETE FROM "quotes" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quotes" rows (missing "clients")', rc; END IF;
    UPDATE "quotes" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "quotes"."job_id"', rc; END IF;
    DELETE FROM "quote_options" c WHERE c."quote_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "quotes" p WHERE p."id" = c."quote_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quote_options" rows (missing "quotes")', rc; END IF;
    DELETE FROM "quote_line_items" c WHERE c."quote_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "quotes" p WHERE p."id" = c."quote_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quote_line_items" rows (missing "quotes")', rc; END IF;
    DELETE FROM "quote_line_items" c WHERE c."option_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "quote_options" p WHERE p."id" = c."option_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quote_line_items" rows (missing "quote_options")', rc; END IF;
    DELETE FROM "quote_versions" c WHERE c."quote_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "quotes" p WHERE p."id" = c."quote_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quote_versions" rows (missing "quotes")', rc; END IF;
    DELETE FROM "invoices" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoices" rows (missing "users")', rc; END IF;
    DELETE FROM "invoices" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoices" rows (missing "clients")', rc; END IF;
    UPDATE "invoices" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "invoices"."job_id"', rc; END IF;
    UPDATE "invoices" c SET "quote_id" = NULL WHERE c."quote_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "quotes" p WHERE p."id" = c."quote_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "invoices"."quote_id"', rc; END IF;
    DELETE FROM "invoice_line_items" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoice_line_items" rows (missing "invoices")', rc; END IF;
    DELETE FROM "payment_requests" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_requests" rows (missing "users")', rc; END IF;
    UPDATE "payment_requests" c SET "invoice_id" = NULL WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "payment_requests"."invoice_id"', rc; END IF;
    UPDATE "payment_requests" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "payment_requests"."job_id"', rc; END IF;
    UPDATE "payment_requests" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "payment_requests"."client_id"', rc; END IF;
    DELETE FROM "receipts" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "receipts" rows (missing "users")', rc; END IF;
    UPDATE "receipts" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "receipts"."job_id"', rc; END IF;
    UPDATE "receipts" c SET "invoice_id" = NULL WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "receipts"."invoice_id"', rc; END IF;
    UPDATE "receipts" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "receipts"."client_id"', rc; END IF;
    UPDATE "receipts" c SET "payment_request_id" = NULL WHERE c."payment_request_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "payment_requests" p WHERE p."id" = c."payment_request_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "receipts"."payment_request_id"', rc; END IF;
    DELETE FROM "rebates" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "rebates" rows (missing "users")', rc; END IF;
    UPDATE "rebates" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "rebates"."client_id"', rc; END IF;
    UPDATE "rebates" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "rebates"."job_id"', rc; END IF;
    UPDATE "rebates" c SET "invoice_id" = NULL WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "rebates"."invoice_id"', rc; END IF;
    DELETE FROM "document_templates" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "document_templates" rows (missing "users")', rc; END IF;
    DELETE FROM "line_item_catalog" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "line_item_catalog" rows (missing "users")', rc; END IF;
    DELETE FROM "rate_cards" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "rate_cards" rows (missing "users")', rc; END IF;
    DELETE FROM "quote_templates" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quote_templates" rows (missing "users")', rc; END IF;
    DELETE FROM "style_presets" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "style_presets" rows (missing "users")', rc; END IF;
    DELETE FROM "template_analysis_jobs" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "template_analysis_jobs" rows (missing "users")', rc; END IF;
    UPDATE "template_analysis_jobs" c SET "created_template_id" = NULL WHERE c."created_template_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "document_templates" p WHERE p."id" = c."created_template_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "template_analysis_jobs"."created_template_id"', rc; END IF;
    DELETE FROM "inventory_categories" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "inventory_categories" rows (missing "users")', rc; END IF;
    DELETE FROM "inventory_items" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "inventory_items" rows (missing "users")', rc; END IF;
    UPDATE "inventory_items" c SET "category_id" = NULL WHERE c."category_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "inventory_categories" p WHERE p."id" = c."category_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "inventory_items"."category_id"', rc; END IF;
    DELETE FROM "inventory_transactions" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "inventory_transactions" rows (missing "users")', rc; END IF;
    DELETE FROM "inventory_transactions" c WHERE c."item_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "inventory_items" p WHERE p."id" = c."item_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "inventory_transactions" rows (missing "inventory_items")', rc; END IF;
    DELETE FROM "inventory_transactions" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "inventory_transactions" rows (missing "jobs")', rc; END IF;
    DELETE FROM "time_entries" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "time_entries" rows (missing "users")', rc; END IF;
    DELETE FROM "time_entries" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "time_entries" rows (missing "jobs")', rc; END IF;
    UPDATE "time_entries" c SET "approved_by" = NULL WHERE c."approved_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."approved_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "time_entries"."approved_by"', rc; END IF;
    DELETE FROM "timesheets" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "timesheets" rows (missing "users")', rc; END IF;
    UPDATE "timesheets" c SET "approved_by" = NULL WHERE c."approved_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."approved_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "timesheets"."approved_by"', rc; END IF;
    DELETE FROM "time_entry_edits" c WHERE c."time_entry_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "time_entries" p WHERE p."id" = c."time_entry_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "time_entry_edits" rows (missing "time_entries")', rc; END IF;
    DELETE FROM "time_entry_edits" c WHERE c."edited_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."edited_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "time_entry_edits" rows (missing "users")', rc; END IF;
    DELETE FROM "invoice_edits" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoice_edits" rows (missing "invoices")', rc; END IF;
    DELETE FROM "invoice_edits" c WHERE c."edited_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."edited_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoice_edits" rows (missing "users")', rc; END IF;
    DELETE FROM "time_entry_dispute_events" c WHERE c."time_entry_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "time_entries" p WHERE p."id" = c."time_entry_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "time_entry_dispute_events" rows (missing "time_entries")', rc; END IF;
    DELETE FROM "time_entry_dispute_events" c WHERE c."actor_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."actor_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "time_entry_dispute_events" rows (missing "users")', rc; END IF;
    DELETE FROM "expense_categories" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "expense_categories" rows (missing "users")', rc; END IF;
    DELETE FROM "expenses" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "expenses" rows (missing "users")', rc; END IF;
    DELETE FROM "expenses" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "expenses" rows (missing "jobs")', rc; END IF;
    DELETE FROM "expenses" c WHERE c."category_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "expense_categories" p WHERE p."id" = c."category_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "expenses" rows (missing "expense_categories")', rc; END IF;
    UPDATE "expenses" c SET "approved_by" = NULL WHERE c."approved_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."approved_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "expenses"."approved_by"', rc; END IF;
    DELETE FROM "team_members" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_members" rows (missing "users")', rc; END IF;
    DELETE FROM "team_members" c WHERE c."member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_members" rows (missing "users")', rc; END IF;
    DELETE FROM "team_members" c WHERE c."role_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "user_roles" p WHERE p."id" = c."role_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_members" rows (missing "user_roles")', rc; END IF;
    DELETE FROM "invite_codes" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invite_codes" rows (missing "users")', rc; END IF;
    UPDATE "invite_codes" c SET "role_id" = NULL WHERE c."role_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "user_roles" p WHERE p."id" = c."role_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "invite_codes"."role_id"', rc; END IF;
    DELETE FROM "job_assignments" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_assignments" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_assignments" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_assignments" rows (missing "users")', rc; END IF;
    UPDATE "job_assignments" c SET "team_member_id" = NULL WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_assignments"."team_member_id"', rc; END IF;
    DELETE FROM "staff_schedules" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "staff_schedules" rows (missing "users")', rc; END IF;
    DELETE FROM "staff_schedules" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "staff_schedules" rows (missing "jobs")', rc; END IF;
    DELETE FROM "team_member_skills" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_member_skills" rows (missing "team_members")', rc; END IF;
    DELETE FROM "team_member_availability" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_member_availability" rows (missing "team_members")', rc; END IF;
    DELETE FROM "team_member_time_off" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_member_time_off" rows (missing "team_members")', rc; END IF;
    UPDATE "team_member_time_off" c SET "approved_by" = NULL WHERE c."approved_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."approved_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "team_member_time_off"."approved_by"', rc; END IF;
    DELETE FROM "permission_requests" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "permission_requests" rows (missing "team_members")', rc; END IF;
    DELETE FROM "permission_requests" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "permission_requests" rows (missing "users")', rc; END IF;
    UPDATE "permission_requests" c SET "responded_by" = NULL WHERE c."responded_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."responded_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "permission_requests"."responded_by"', rc; END IF;
    DELETE FROM "job_assignment_requests" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_assignment_requests" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_assignment_requests" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_assignment_requests" rows (missing "team_members")', rc; END IF;
    DELETE FROM "job_assignment_requests" c WHERE c."requester_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."requester_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_assignment_requests" rows (missing "users")', rc; END IF;
    DELETE FROM "job_assignment_requests" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_assignment_requests" rows (missing "users")', rc; END IF;
    UPDATE "job_assignment_requests" c SET "responded_by" = NULL WHERE c."responded_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."responded_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_assignment_requests"."responded_by"', rc; END IF;
    DELETE FROM "team_member_metrics" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_member_metrics" rows (missing "team_members")', rc; END IF;
    DELETE FROM "location_tracking" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "location_tracking" rows (missing "users")', rc; END IF;
    DELETE FROM "location_tracking" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "location_tracking" rows (missing "jobs")', rc; END IF;
    DELETE FROM "geofence_alerts" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "geofence_alerts" rows (missing "users")', rc; END IF;
    DELETE FROM "geofence_alerts" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "geofence_alerts" rows (missing "jobs")', rc; END IF;
    DELETE FROM "geofence_alerts" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "geofence_alerts" rows (missing "users")', rc; END IF;
    DELETE FROM "gps_signal_logs" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "gps_signal_logs" rows (missing "users")', rc; END IF;
    DELETE FROM "gps_signal_logs" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "gps_signal_logs" rows (missing "users")', rc; END IF;
    UPDATE "gps_signal_logs" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "gps_signal_logs"."job_id"', rc; END IF;
    DELETE FROM "tradie_status" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "tradie_status" rows (missing "users")', rc; END IF;
    DELETE FROM "tradie_status" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "tradie_status" rows (missing "users")', rc; END IF;
    UPDATE "tradie_status" c SET "current_job_id" = NULL WHERE c."current_job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."current_job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "tradie_status"."current_job_id"', rc; END IF;
    DELETE FROM "routes" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "routes" rows (missing "users")', rc; END IF;
    DELETE FROM "customer_users" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "customer_users" rows (missing "clients")', rc; END IF;
    DELETE FROM "customer_users" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "customer_users" rows (missing "users")', rc; END IF;
    DELETE FROM "customer_sessions" c WHERE c."customer_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "customer_users" p WHERE p."id" = c."customer_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "customer_sessions" rows (missing "customer_users")', rc; END IF;
    DELETE FROM "equipment_categories" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "equipment_categories" rows (missing "users")', rc; END IF;
    DELETE FROM "equipment" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "equipment" rows (missing "users")', rc; END IF;
    UPDATE "equipment" c SET "category_id" = NULL WHERE c."category_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "equipment_categories" p WHERE p."id" = c."category_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "equipment"."category_id"', rc; END IF;
    UPDATE "equipment" c SET "assigned_to" = NULL WHERE c."assigned_to" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."assigned_to");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "equipment"."assigned_to"', rc; END IF;
    DELETE FROM "equipment_maintenance" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "equipment_maintenance" rows (missing "users")', rc; END IF;
    DELETE FROM "equipment_maintenance" c WHERE c."equipment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "equipment" p WHERE p."id" = c."equipment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "equipment_maintenance" rows (missing "equipment")', rc; END IF;
    UPDATE "equipment_maintenance" c SET "performed_by" = NULL WHERE c."performed_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."performed_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "equipment_maintenance"."performed_by"', rc; END IF;
    DELETE FROM "client_assets" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "client_assets" rows (missing "users")', rc; END IF;
    DELETE FROM "client_assets" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "client_assets" rows (missing "clients")', rc; END IF;
    DELETE FROM "client_asset_services" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "client_asset_services" rows (missing "users")', rc; END IF;
    DELETE FROM "client_asset_services" c WHERE c."asset_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "client_assets" p WHERE p."id" = c."asset_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "client_asset_services" rows (missing "client_assets")', rc; END IF;
    UPDATE "client_asset_services" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "client_asset_services"."job_id"', rc; END IF;
    UPDATE "client_asset_services" c SET "performed_by" = NULL WHERE c."performed_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."performed_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "client_asset_services"."performed_by"', rc; END IF;
    DELETE FROM "recurring_contracts" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "recurring_contracts" rows (missing "users")', rc; END IF;
    DELETE FROM "recurring_contracts" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "recurring_contracts" rows (missing "clients")', rc; END IF;
    DELETE FROM "recurring_schedules" c WHERE c."contract_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "recurring_contracts" p WHERE p."id" = c."contract_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "recurring_schedules" rows (missing "recurring_contracts")', rc; END IF;
    DELETE FROM "recurring_schedules" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "recurring_schedules" rows (missing "jobs")', rc; END IF;
    DELETE FROM "custom_forms" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "custom_forms" rows (missing "users")', rc; END IF;
    DELETE FROM "form_submissions" c WHERE c."form_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "custom_forms" p WHERE p."id" = c."form_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "form_submissions" rows (missing "custom_forms")', rc; END IF;
    DELETE FROM "form_submissions" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "form_submissions" rows (missing "jobs")', rc; END IF;
    UPDATE "form_submissions" c SET "submitted_by" = NULL WHERE c."submitted_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."submitted_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "form_submissions"."submitted_by"', rc; END IF;
    UPDATE "form_submissions" c SET "customer_user_id" = NULL WHERE c."customer_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "customer_users" p WHERE p."id" = c."customer_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "form_submissions"."customer_user_id"', rc; END IF;
    UPDATE "form_submissions" c SET "reviewed_by" = NULL WHERE c."reviewed_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."reviewed_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "form_submissions"."reviewed_by"', rc; END IF;
    DELETE FROM "digital_signatures" c WHERE c."form_submission_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "form_submissions" p WHERE p."id" = c."form_submission_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "digital_signatures" rows (missing "form_submissions")', rc; END IF;
    DELETE FROM "digital_signatures" c WHERE c."assignment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_assignments" p WHERE p."id" = c."assignment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "digital_signatures" rows (missing "job_assignments")', rc; END IF;
    DELETE FROM "digital_signatures" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "digital_signatures" rows (missing "jobs")', rc; END IF;
    DELETE FROM "digital_signatures" c WHERE c."quote_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "quotes" p WHERE p."id" = c."quote_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "digital_signatures" rows (missing "quotes")', rc; END IF;
    DELETE FROM "digital_signatures" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "digital_signatures" rows (missing "invoices")', rc; END IF;
    UPDATE "digital_signatures" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "digital_signatures"."client_id"', rc; END IF;
    DELETE FROM "email_campaigns" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "email_campaigns" rows (missing "users")', rc; END IF;
    DELETE FROM "customer_surveys" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "customer_surveys" rows (missing "users")', rc; END IF;
    DELETE FROM "customer_surveys" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "customer_surveys" rows (missing "jobs")', rc; END IF;
    DELETE FROM "customer_surveys" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "customer_surveys" rows (missing "clients")', rc; END IF;
    DELETE FROM "suppliers" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "suppliers" rows (missing "users")', rc; END IF;
    DELETE FROM "purchase_orders" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "purchase_orders" rows (missing "users")', rc; END IF;
    DELETE FROM "purchase_orders" c WHERE c."supplier_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "suppliers" p WHERE p."id" = c."supplier_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "purchase_orders" rows (missing "suppliers")', rc; END IF;
    DELETE FROM "purchase_orders" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "purchase_orders" rows (missing "jobs")', rc; END IF;
    UPDATE "purchase_orders" c SET "approved_by" = NULL WHERE c."approved_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."approved_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "purchase_orders"."approved_by"', rc; END IF;
    DELETE FROM "purchase_order_items" c WHERE c."po_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "purchase_orders" p WHERE p."id" = c."po_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "purchase_order_items" rows (missing "purchase_orders")', rc; END IF;
    DELETE FROM "purchase_order_items" c WHERE c."inventory_item_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "inventory_items" p WHERE p."id" = c."inventory_item_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "purchase_order_items" rows (missing "inventory_items")', rc; END IF;
    DELETE FROM "report_configurations" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "report_configurations" rows (missing "users")', rc; END IF;
    DELETE FROM "saved_reports" c WHERE c."config_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "report_configurations" p WHERE p."id" = c."config_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "saved_reports" rows (missing "report_configurations")', rc; END IF;
    DELETE FROM "saved_reports" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "saved_reports" rows (missing "users")', rc; END IF;
    DELETE FROM "email_integrations" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "email_integrations" rows (missing "users")', rc; END IF;
    DELETE FROM "email_delivery_logs" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "email_delivery_logs" rows (missing "users")', rc; END IF;
    UPDATE "email_delivery_logs" c SET "email_integration_id" = NULL WHERE c."email_integration_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "email_integrations" p WHERE p."id" = c."email_integration_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "email_delivery_logs"."email_integration_id"', rc; END IF;
    DELETE FROM "job_photos" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_photos" rows (missing "users")', rc; END IF;
    DELETE FROM "job_photos" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_photos" rows (missing "jobs")', rc; END IF;
    UPDATE "job_photos" c SET "uploaded_by" = NULL WHERE c."uploaded_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."uploaded_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_photos"."uploaded_by"', rc; END IF;
    DELETE FROM "voice_notes" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "voice_notes" rows (missing "users")', rc; END IF;
    DELETE FROM "voice_notes" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "voice_notes" rows (missing "jobs")', rc; END IF;
    UPDATE "voice_notes" c SET "recorded_by" = NULL WHERE c."recorded_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."recorded_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "voice_notes"."recorded_by"', rc; END IF;
    DELETE FROM "job_notes" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_notes" rows (missing "users")', rc; END IF;
    DELETE FROM "job_notes" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_notes" rows (missing "jobs")', rc; END IF;
    UPDATE "job_notes" c SET "created_by" = NULL WHERE c."created_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."created_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_notes"."created_by"', rc; END IF;
    DELETE FROM "job_variations" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_variations" rows (missing "users")', rc; END IF;
    DELETE FROM "job_variations" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_variations" rows (missing "jobs")', rc; END IF;
    UPDATE "job_variations" c SET "created_by" = NULL WHERE c."created_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."created_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_variations"."created_by"', rc; END IF;
    DELETE FROM "job_documents" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_documents" rows (missing "users")', rc; END IF;
    DELETE FROM "job_documents" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_documents" rows (missing "jobs")', rc; END IF;
    UPDATE "job_documents" c SET "uploaded_by" = NULL WHERE c."uploaded_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."uploaded_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_documents"."uploaded_by"', rc; END IF;
    DELETE FROM "invoice_reminder_logs" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoice_reminder_logs" rows (missing "invoices")', rc; END IF;
    DELETE FROM "invoice_reminder_logs" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "invoice_reminder_logs" rows (missing "users")', rc; END IF;
    DELETE FROM "stripe_payouts" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "stripe_payouts" rows (missing "users")', rc; END IF;
    UPDATE "stripe_payouts" c SET "invoice_id" = NULL WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "stripe_payouts"."invoice_id"', rc; END IF;
    DELETE FROM "job_chat" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_chat" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_chat" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_chat" rows (missing "users")', rc; END IF;
    DELETE FROM "team_chat" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_chat" rows (missing "users")', rc; END IF;
    DELETE FROM "team_chat" c WHERE c."sender_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."sender_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_chat" rows (missing "users")', rc; END IF;
    DELETE FROM "direct_messages" c WHERE c."sender_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."sender_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "direct_messages" rows (missing "users")', rc; END IF;
    DELETE FROM "direct_messages" c WHERE c."recipient_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."recipient_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "direct_messages" rows (missing "users")', rc; END IF;
    DELETE FROM "sms_conversations" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_conversations" rows (missing "users")', rc; END IF;
    UPDATE "sms_conversations" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_conversations"."client_id"', rc; END IF;
    UPDATE "sms_conversations" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_conversations"."job_id"', rc; END IF;
    DELETE FROM "sms_messages" c WHERE c."conversation_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "sms_conversations" p WHERE p."id" = c."conversation_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_messages" rows (missing "sms_conversations")', rc; END IF;
    UPDATE "sms_messages" c SET "sender_user_id" = NULL WHERE c."sender_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."sender_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_messages"."sender_user_id"', rc; END IF;
    UPDATE "sms_messages" c SET "job_created_from_sms" = NULL WHERE c."job_created_from_sms" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_created_from_sms");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_messages"."job_created_from_sms"', rc; END IF;
    DELETE FROM "automations" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "automations" rows (missing "users")', rc; END IF;
    DELETE FROM "automation_logs" c WHERE c."automation_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "automations" p WHERE p."id" = c."automation_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "automation_logs" rows (missing "automations")', rc; END IF;
    DELETE FROM "sms_templates" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_templates" rows (missing "users")', rc; END IF;
    DELETE FROM "quick_replies" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quick_replies" rows (missing "users")', rc; END IF;
    DELETE FROM "business_templates" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "business_templates" rows (missing "users")', rc; END IF;
    DELETE FROM "sms_booking_links" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_booking_links" rows (missing "jobs")', rc; END IF;
    DELETE FROM "sms_booking_links" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_booking_links" rows (missing "users")', rc; END IF;
    DELETE FROM "sms_tracking_links" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_tracking_links" rows (missing "jobs")', rc; END IF;
    UPDATE "sms_tracking_links" c SET "team_member_id" = NULL WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_tracking_links"."team_member_id"', rc; END IF;
    DELETE FROM "sms_tracking_links" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_tracking_links" rows (missing "users")', rc; END IF;
    DELETE FROM "sms_automation_rules" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_automation_rules" rows (missing "users")', rc; END IF;
    UPDATE "sms_automation_rules" c SET "template_id" = NULL WHERE c."template_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "sms_templates" p WHERE p."id" = c."template_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_automation_rules"."template_id"', rc; END IF;
    DELETE FROM "sms_automation_logs" c WHERE c."rule_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "sms_automation_rules" p WHERE p."id" = c."rule_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_automation_logs" rows (missing "sms_automation_rules")', rc; END IF;
    UPDATE "sms_automation_logs" c SET "message_id" = NULL WHERE c."message_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "sms_messages" p WHERE p."id" = c."message_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_automation_logs"."message_id"', rc; END IF;
    DELETE FROM "xero_connections" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "xero_connections" rows (missing "users")', rc; END IF;
    DELETE FROM "xero_sync_state" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "xero_sync_state" rows (missing "users")', rc; END IF;
    DELETE FROM "myob_connections" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "myob_connections" rows (missing "users")', rc; END IF;
    DELETE FROM "quickbooks_connections" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "quickbooks_connections" rows (missing "users")', rc; END IF;
    DELETE FROM "job_reminders" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_reminders" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_reminders" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_reminders" rows (missing "users")', rc; END IF;
    DELETE FROM "job_photo_requirements" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_photo_requirements" rows (missing "jobs")', rc; END IF;
    DELETE FROM "defects" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "defects" rows (missing "jobs")', rc; END IF;
    DELETE FROM "defects" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "defects" rows (missing "users")', rc; END IF;
    UPDATE "defects" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "defects"."client_id"', rc; END IF;
    DELETE FROM "timesheet_approvals" c WHERE c."time_entry_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "time_entries" p WHERE p."id" = c."time_entry_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "timesheet_approvals" rows (missing "time_entries")', rc; END IF;
    DELETE FROM "timesheet_approvals" c WHERE c."submitted_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."submitted_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "timesheet_approvals" rows (missing "users")', rc; END IF;
    UPDATE "timesheet_approvals" c SET "approved_by" = NULL WHERE c."approved_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."approved_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "timesheet_approvals"."approved_by"', rc; END IF;
    DELETE FROM "job_materials" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_materials" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_materials" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_materials" rows (missing "users")', rc; END IF;
    DELETE FROM "job_equipment" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_equipment" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_equipment" c WHERE c."equipment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "equipment" p WHERE p."id" = c."equipment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_equipment" rows (missing "equipment")', rc; END IF;
    DELETE FROM "job_equipment" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_equipment" rows (missing "users")', rc; END IF;
    DELETE FROM "automation_settings" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "automation_settings" rows (missing "users")', rc; END IF;
    DELETE FROM "terminal_locations" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "terminal_locations" rows (missing "users")', rc; END IF;
    DELETE FROM "terminal_payments" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "terminal_payments" rows (missing "users")', rc; END IF;
    UPDATE "terminal_payments" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "terminal_payments"."client_id"', rc; END IF;
    UPDATE "terminal_payments" c SET "invoice_id" = NULL WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "terminal_payments"."invoice_id"', rc; END IF;
    UPDATE "terminal_payments" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "terminal_payments"."job_id"', rc; END IF;
    UPDATE "terminal_payments" c SET "location_id" = NULL WHERE c."location_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "terminal_locations" p WHERE p."id" = c."location_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "terminal_payments"."location_id"', rc; END IF;
    DELETE FROM "tap_to_pay_terms_acceptance" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "tap_to_pay_terms_acceptance" rows (missing "users")', rc; END IF;
    DELETE FROM "tap_to_pay_terms_acceptance" c WHERE c."accepted_by_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."accepted_by_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "tap_to_pay_terms_acceptance" rows (missing "users")', rc; END IF;
    DELETE FROM "leads" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "leads" rows (missing "users")', rc; END IF;
    UPDATE "leads" c SET "client_id" = NULL WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "leads"."client_id"', rc; END IF;
    DELETE FROM "payment_schedules" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_schedules" rows (missing "users")', rc; END IF;
    DELETE FROM "payment_schedules" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_schedules" rows (missing "invoices")', rc; END IF;
    DELETE FROM "payment_schedules" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_schedules" rows (missing "clients")', rc; END IF;
    DELETE FROM "payment_installments" c WHERE c."schedule_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "payment_schedules" p WHERE p."id" = c."schedule_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_installments" rows (missing "payment_schedules")', rc; END IF;
    DELETE FROM "team_groups" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_groups" rows (missing "users")', rc; END IF;
    DELETE FROM "team_group_members" c WHERE c."group_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_groups" p WHERE p."id" = c."group_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_group_members" rows (missing "team_groups")', rc; END IF;
    DELETE FROM "team_group_members" c WHERE c."team_member_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "team_members" p WHERE p."id" = c."team_member_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "team_group_members" rows (missing "team_members")', rc; END IF;
    DELETE FROM "job_invites" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_invites" rows (missing "jobs")', rc; END IF;
    DELETE FROM "job_invites" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_invites" rows (missing "users")', rc; END IF;
    UPDATE "job_invites" c SET "used_by" = NULL WHERE c."used_by" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."used_by");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_invites"."used_by"', rc; END IF;
    DELETE FROM "job_portal_tokens" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_portal_tokens" rows (missing "jobs")', rc; END IF;
    UPDATE "job_portal_tokens" c SET "assignment_id" = NULL WHERE c."assignment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_assignments" p WHERE p."id" = c."assignment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_portal_tokens"."assignment_id"', rc; END IF;
    DELETE FROM "job_portal_tokens" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_portal_tokens" rows (missing "users")', rc; END IF;
    DELETE FROM "sms_notification_log" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_notification_log" rows (missing "jobs")', rc; END IF;
    UPDATE "sms_notification_log" c SET "assignment_id" = NULL WHERE c."assignment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_assignments" p WHERE p."id" = c."assignment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_notification_log"."assignment_id"', rc; END IF;
    DELETE FROM "sms_notification_log" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "sms_notification_log" rows (missing "users")', rc; END IF;
    UPDATE "sms_notification_log" c SET "sms_message_id" = NULL WHERE c."sms_message_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "sms_messages" p WHERE p."id" = c."sms_message_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_notification_log"."sms_message_id"', rc; END IF;
    UPDATE "sms_notification_log" c SET "portal_token_id" = NULL WHERE c."portal_token_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_portal_tokens" p WHERE p."id" = c."portal_token_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "sms_notification_log"."portal_token_id"', rc; END IF;
    DELETE FROM "assignment_events" c WHERE c."assignment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_assignments" p WHERE p."id" = c."assignment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "assignment_events" rows (missing "job_assignments")', rc; END IF;
    DELETE FROM "assignment_events" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "assignment_events" rows (missing "jobs")', rc; END IF;
    DELETE FROM "assignment_events" c WHERE c."actor_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."actor_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "assignment_events" rows (missing "users")', rc; END IF;
    DELETE FROM "location_pings" c WHERE c."assignment_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_assignments" p WHERE p."id" = c."assignment_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "location_pings" rows (missing "job_assignments")', rc; END IF;
    DELETE FROM "location_pings" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "location_pings" rows (missing "users")', rc; END IF;
    DELETE FROM "subcontractor_tokens" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_tokens" rows (missing "jobs")', rc; END IF;
    UPDATE "subcontractor_tokens" c SET "invite_id" = NULL WHERE c."invite_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "job_invites" p WHERE p."id" = c."invite_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "subcontractor_tokens"."invite_id"', rc; END IF;
    DELETE FROM "subcontractor_tokens" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_tokens" rows (missing "users")', rc; END IF;
    UPDATE "subcontractor_tokens" c SET "recipient_user_id" = NULL WHERE c."recipient_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."recipient_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "subcontractor_tokens"."recipient_user_id"', rc; END IF;
    DELETE FROM "subcontractor_sessions" c WHERE c."token_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "subcontractor_tokens" p WHERE p."id" = c."token_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_sessions" rows (missing "subcontractor_tokens")', rc; END IF;
    DELETE FROM "subcontractor_events" c WHERE c."token_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "subcontractor_tokens" p WHERE p."id" = c."token_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_events" rows (missing "subcontractor_tokens")', rc; END IF;
    DELETE FROM "subcontractor_events" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_events" rows (missing "jobs")', rc; END IF;
    DELETE FROM "subcontractor_location_pings" c WHERE c."token_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "subcontractor_tokens" p WHERE p."id" = c."token_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_location_pings" rows (missing "subcontractor_tokens")', rc; END IF;
    DELETE FROM "subcontractor_location_pings" c WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_location_pings" rows (missing "jobs")', rc; END IF;
    DELETE FROM "worker_requests" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "worker_requests" rows (missing "clients")', rc; END IF;
    DELETE FROM "worker_requests" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "worker_requests" rows (missing "users")', rc; END IF;
    DELETE FROM "worker_requests" c WHERE c."preferred_worker_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."preferred_worker_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "worker_requests" rows (missing "users")', rc; END IF;
    UPDATE "worker_requests" c SET "reference_job_id" = NULL WHERE c."reference_job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."reference_job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "worker_requests"."reference_job_id"', rc; END IF;
    DELETE FROM "compliance_documents" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "compliance_documents" rows (missing "users")', rc; END IF;
    UPDATE "compliance_documents" c SET "holder_user_id" = NULL WHERE c."holder_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."holder_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "compliance_documents"."holder_user_id"', rc; END IF;
    DELETE FROM "job_requests" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_requests" rows (missing "users")', rc; END IF;
    DELETE FROM "job_requests" c WHERE c."client_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "clients" p WHERE p."id" = c."client_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "job_requests" rows (missing "clients")', rc; END IF;
    UPDATE "job_requests" c SET "reference_job_id" = NULL WHERE c."reference_job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."reference_job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_requests"."reference_job_id"', rc; END IF;
    UPDATE "job_requests" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "job_requests"."job_id"', rc; END IF;
    DELETE FROM "saved_filters" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "saved_filters" rows (missing "users")', rc; END IF;
    DELETE FROM "payment_records" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_records" rows (missing "invoices")', rc; END IF;
    DELETE FROM "payment_records" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "payment_records" rows (missing "users")', rc; END IF;
    DELETE FROM "swms_documents" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "swms_documents" rows (missing "users")', rc; END IF;
    UPDATE "swms_documents" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "swms_documents"."job_id"', rc; END IF;
    DELETE FROM "swms_hazards" c WHERE c."swms_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "swms_documents" p WHERE p."id" = c."swms_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "swms_hazards" rows (missing "swms_documents")', rc; END IF;
    DELETE FROM "swms_signatures" c WHERE c."swms_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "swms_documents" p WHERE p."id" = c."swms_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "swms_signatures" rows (missing "swms_documents")', rc; END IF;
    UPDATE "swms_signatures" c SET "worker_user_id" = NULL WHERE c."worker_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."worker_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "swms_signatures"."worker_user_id"', rc; END IF;
    DELETE FROM "incident_reports" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "incident_reports" rows (missing "users")', rc; END IF;
    UPDATE "incident_reports" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "incident_reports"."job_id"', rc; END IF;
    DELETE FROM "site_emergency_info" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "site_emergency_info" rows (missing "users")', rc; END IF;
    UPDATE "site_emergency_info" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "site_emergency_info"."job_id"', rc; END IF;
    DELETE FROM "jsa_documents" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "jsa_documents" rows (missing "users")', rc; END IF;
    UPDATE "jsa_documents" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "jsa_documents"."job_id"', rc; END IF;
    DELETE FROM "jsa_steps" c WHERE c."jsa_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jsa_documents" p WHERE p."id" = c."jsa_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "jsa_steps" rows (missing "jsa_documents")', rc; END IF;
    DELETE FROM "site_hazardous_environments" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "site_hazardous_environments" rows (missing "users")', rc; END IF;
    UPDATE "site_hazardous_environments" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "site_hazardous_environments"."job_id"', rc; END IF;
    DELETE FROM "site_safety_signage" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "site_safety_signage" rows (missing "users")', rc; END IF;
    UPDATE "site_safety_signage" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "site_safety_signage"."job_id"', rc; END IF;
    DELETE FROM "hazard_reports" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "hazard_reports" rows (missing "users")', rc; END IF;
    UPDATE "hazard_reports" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "hazard_reports"."job_id"', rc; END IF;
    DELETE FROM "ppe_checklists" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "ppe_checklists" rows (missing "users")', rc; END IF;
    UPDATE "ppe_checklists" c SET "job_id" = NULL WHERE c."job_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "jobs" p WHERE p."id" = c."job_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'nulled % orphaned "ppe_checklists"."job_id"', rc; END IF;
    DELETE FROM "training_records" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "training_records" rows (missing "users")', rc; END IF;
    DELETE FROM "ai_receptionist_config" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "ai_receptionist_config" rows (missing "users")', rc; END IF;
    DELETE FROM "ai_receptionist_calls" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "ai_receptionist_calls" rows (missing "users")', rc; END IF;
    DELETE FROM "audit_logs" c WHERE c."admin_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."admin_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "audit_logs" rows (missing "users")', rc; END IF;
    DELETE FROM "audit_logs" c WHERE c."target_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."target_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "audit_logs" rows (missing "users")', rc; END IF;
    DELETE FROM "website_addons" c WHERE c."business_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "website_addons" rows (missing "users")', rc; END IF;
    DELETE FROM "website_change_requests" c WHERE c."business_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "website_change_requests" rows (missing "users")', rc; END IF;
    DELETE FROM "website_change_requests" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "website_change_requests" rows (missing "users")', rc; END IF;
    DELETE FROM "voice_change_requests" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "voice_change_requests" rows (missing "users")', rc; END IF;
    DELETE FROM "subcontractor_invoices" c WHERE c."subcontractor_user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."subcontractor_user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_invoices" rows (missing "users")', rc; END IF;
    DELETE FROM "subcontractor_invoices" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_invoices" rows (missing "users")', rc; END IF;
    DELETE FROM "subcontractor_invoice_items" c WHERE c."invoice_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "subcontractor_invoices" p WHERE p."id" = c."invoice_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "subcontractor_invoice_items" rows (missing "subcontractor_invoices")', rc; END IF;
    DELETE FROM "worker_states" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "worker_states" rows (missing "users")', rc; END IF;
    DELETE FROM "worker_states" c WHERE c."business_owner_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."business_owner_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "worker_states" rows (missing "users")', rc; END IF;
    DELETE FROM "number_port_requests" c WHERE c."user_id" IS NOT NULL AND NOT EXISTS (SELECT 1 FROM "users" p WHERE p."id" = c."user_id");
    GET DIAGNOSTICS rc = ROW_COUNT; total := total + rc;
    IF rc > 0 THEN RAISE NOTICE 'deleted % orphaned "number_port_requests" rows (missing "users")', rc; END IF;
    IF total > MAX_PER_PASS THEN
      RAISE EXCEPTION 'orphan cleanup touched % rows in one pass (> %); aborting as a safety guard', total, MAX_PER_PASS;
    END IF;
    EXIT WHEN total = 0 OR passes >= 12;
  END LOOP;
  RAISE NOTICE 'orphan cleanup finished in % pass(es)', passes;
END $$;
